# Dark mode for Dashboard and Admin

## Changes
- Activate the existing saved theme system around the app so the toggle can switch and remember light or dark mode.
- Add a clearly labelled sun/moon toggle to the Dashboard header and Admin header.
- Add dark-theme colour tokens and replace the Dashboard’s fixed white section backgrounds with theme-aware surfaces.
- Keep all public-page layouts and content unchanged.

## Verification
- Check the toggle changes the page immediately, persists after reload, and remains usable on mobile-sized screens.
- Run the project typecheck and inspect both private-page headers where access permits.
